export function SiteFooter() {
  return (
    <footer className="mt-20 border-t border-line bg-panel">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-mute sm:px-6 md:flex-row md:items-center md:justify-between">
        <span>星港 StarPort — 一站式 AI 应用、模型与 Agent 平台</span>
        <span>Phase 0.5 原型 · 路线图见 docs/superpowers/specs</span>
      </div>
    </footer>
  );
}
